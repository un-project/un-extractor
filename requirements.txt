# Core pipeline
pymupdf>=1.24.0
pydantic>=2.0.0
anthropic>=0.25.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Data / thesaurus (rdflib only needed to regenerate unbis_subjects.py)
rdflib>=6.0.0

# Ideal point estimation (compute_ideal_points.py)
numpy>=1.26.0
scipy>=1.12.0

# Vote prediction and anomaly detection (compute_vote_predictions.py)
# Topic modelling (compute_speech_topics.py — LDA backend)
scikit-learn>=1.4.0

# Canonical name resolution (fix_country_duplicates.py)
pycountry>=26.2.0

# Optional: BERTopic backend for compute_speech_topics.py
# pip install bertopic sentence-transformers

# Dev / quality
black>=24.0.0
flake8>=7.0.0
mypy>=1.10.0
pytest>=8.0.0
pytest-cov>=4.0.0
types-psycopg2>=2.9.0
