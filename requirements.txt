# Core pipeline
pymupdf>=1.24.0
pydantic>=2.0.0
anthropic>=0.25.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Data / thesaurus (rdflib only needed to regenerate unbis_subjects.py)
rdflib>=6.0.0

# Ideal point estimation (compute_ideal_points.py)
numpy>=1.26.0
scipy>=1.12.0

# Vote prediction and anomaly detection (compute_vote_predictions.py)
scikit-learn>=1.4.0

# Dev / quality
black>=24.0.0
flake8>=7.0.0
mypy>=1.10.0
pytest>=8.0.0
pytest-cov>=4.0.0
types-psycopg2>=2.9.0
