from setuptools import setup

# a minimal stub; all metadata is now maintained in pyproject.toml.
# `setup()` will read the configuration from there when executed.
if __name__ == "__main__":
    setup()
